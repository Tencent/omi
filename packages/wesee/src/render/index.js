const defaultValue = {
	width: 0,
	height: 0,
	color: black
}

export function draw(element, innerWidth, innerHeight){
	const canvas  = document.createElement('canvas')
	//childNodes
	//parentNode
	console.log(element)
	return canvas
}
